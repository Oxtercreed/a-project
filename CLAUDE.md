# Project guidance

Follow `AGENTS.md` for repository-wide instructions.

For every UI/UX task, read and apply `.claude/skills/ui-ux-pro-max/SKILL.md` plus the relevant supporting skill in `.claude/skills/` before editing the interface. Use the bundled search tool for non-trivial visual decisions.
